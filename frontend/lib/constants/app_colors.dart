import 'package:flutter/material.dart';

class AppColors {
  // 🎨 Base Colors
  static const Color base = Color(0xFF11121A);
  static const Color card = Color(0xFF1C1E27);
  static const Color accent = Color(0xFF5E63FF);
  static const Color success = Colors.greenAccent;
  static const Color warning = Colors.yellow;
  static const Color error = Colors.redAccent;

  // 🖋️ Text Colors
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.white70;
  static const Color greyText = Color(0xFFB0B3C1);

  // 🌗 Optional Light Mode colors (for future theme toggle)
  static const Color lightBase = Colors.white;
  static const Color lightCard = Color(0xFFF4F5F9);
  static const Color lightText = Color(0xFF11121A);
}
